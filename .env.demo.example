# Demo env for launchpad-web VM deployment
# Copy this file to .env.demo on the VM and fill values.

# Port exposed on VM
WEB_HOST_PORT=9010

# Build-time API target for Flutter web
API_BASE_URL=http://<vm_public_ip>:8010/api/v1
